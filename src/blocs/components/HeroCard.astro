---
import { Icon } from "astro-icon/components";
const { cardIcon, cardColor, cardTitle, cardContent } = Astro.props;
---

<div
	class={`hero-card hero-card--${cardColor}`}
	role="group"
	aria-label={cardTitle}
>
	<Icon
		name={cardIcon}
		width="55"
		height="55"
	/>
	<p class="hero-card__title h3">
		<strong style="font-weight: 400;">{cardTitle}</strong>
	</p>
	<p class="hero-card__content">
		{cardContent}
	</p>
</div>

<style lang="scss">
	@use "@styles/helpers" as *;

	.hero-card {
		padding: 32px 32px 38px;

		&--yellow {
			background-color: var(--color-yellow);
		}

		&--blue {
			background-color: var(--color-blue-200);
		}

		&--pink {
			background-color: var(--color-pink);
		}

		&__title {
			margin-top: fluid-big(12, 8);
		}

		&__content {
			margin-top: fluid-big(16, 13);

			@include font-variable(18, 15, 14);
		}
	}
</style>
